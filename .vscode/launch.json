{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Express: debug",
      "type": "node-terminal",
      "request": "launch",
      "command": "pnpm dev",
      "cwd": "${workspaceFolder}/apps/express",
      "env": {
        "NODE_ENV": "development"
      }
    }
  ]
}
